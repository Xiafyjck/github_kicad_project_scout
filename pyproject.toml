[project]
name = "pcb-project-scout"
version = "0.1.0"
description = "Discover open-source PCB projects from public repository hosts."
readme = "README.md"
authors = [
    { name = "xiafyjck", email = "xiafyjck@outlook.com" }
]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.uv]
package = false
